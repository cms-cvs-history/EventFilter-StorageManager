process PLAYB = {

  service = MessageLogger {
    untracked vstring destinations = { "cout" }
    //untracked PSet    cout         = { untracked string threshold = "FATAL" }
    untracked PSet    log4cplus    = { untracked string threshold = "WARNING" }
    untracked PSet    cout         = { untracked string threshold = "WARNING" }
    //untracked PSet    cout         = { untracked string threshold = "INFO" }
    //untracked PSet    log4cplus    = { untracked string threshold = "INFO" }
  }

  untracked PSet maxEvents = {untracked int32 input = -1}
  // source
  source = PoolSource
  {
    untracked vstring fileNames = {'file:/scratch2/cheung/fed4playback.root'}
  }

  path p = { playback }

  module playback = PlaybackRawDataProvider
  {
    untracked uint32 queueSize = 32
  }
  
  looper = IterateNTimesLooper {
    uint32 nTimes = 2
  }

}
